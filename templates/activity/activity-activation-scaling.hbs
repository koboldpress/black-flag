<section>
	{{~#if hasScaling}}
	<fieldset>
			<legend>{{ localize "BF.ACTIVATION.SECTION.Scaling" }}</legend>
			{{#if spellSlots}}
				{{ formField spellSlots.field name=spellSlots.name value=spellSlots.value options=spellSlots.options }}
			{{/if}}
			{{#if scaling}}
				{{#blackFlag-formGroup "BF.CONSUMPTION.Scaling.Label" classes="scaling-fields"}}
					{{#if scaling.showRange}}
						<range-picker name="{{ scaling.name }}" value="{{ scaling.value }}" min="1" max="{{ scaling.max }}"
						              step="1"></range-picker>
					{{else}}
						{{ formInput scaling.field name=scaling.name value=scaling.value }}
					{{/if}}
					<span class="separator">/</span>
					<span class="max">{{ blackFlag-number scaling.max }}</span>
				{{/blackFlag-formGroup}}
			{{/if}}
	</fieldset>
	{{> "blackFlag.activity-activation-notes" }}
	{{/if~}}
</section>
