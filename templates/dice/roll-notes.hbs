{{#if rollNotes.length}}
	<fieldset class="roll-notes">
		<legend>Notes</legend>
		{{#each rollNotes}}
			<label>
				<figure class="advantage-mode" data-advantage-mode="{{this.note.rollMode}}">
					<i class="die-icon d20 black"></i>
					<figcaption>{{localize this.advantageAbbreviation}}</figcaption>
				</figure>
				<p>{{this.note.text}}</p>
			</label>
		{{/each}}
	</fieldset>
{{/if}}
