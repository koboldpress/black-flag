<section>
	<ol>
		{{#each types}}
			<li>
				<label>
					<figure>
						<blackFlag-icon class="icon" src="{{#if icon}}{{ icon }}{{else}}{{ img }}{{/if}}"></blackFlag-icon>
					</figure>
					<span class="name">{{ label }}</span>
					<input name="type" type="radio" value="{{ @key }}" {{ disabled disabled }}>
					<p class="hint">{{ hint }}</p>
				</label>
			</li>
		{{/each}}
	</ol>
</section>
